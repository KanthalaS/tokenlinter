"""tokenlinter — Python-native design-token QA.

Validates design tokens (W3C DTCG format) and enforces WCAG contrast so
design systems catch drift and accessibility regressions before they ship.
"""

__version__ = "0.3.0"
